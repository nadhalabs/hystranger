NEXT_PUBLIC_SIGNALING_URL=http://localhost:8000
# Used only as a fallback if the authenticated ICE configuration endpoint is unavailable.
NEXT_PUBLIC_ICE_SERVERS=[{"urls":"stun:stun.l.google.com:19302"}]
